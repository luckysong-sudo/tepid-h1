name: Remote Quality Policy

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  zero-gpu-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm remote-only test policy
        run: |
          echo "All Tepid-H1 tests execute in himartoffice/Tepid-H1 ZeroGPU."
          echo "Use the /run_remote_quality_gate Gradio API and Bucket report."
